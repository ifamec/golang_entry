# IMS

1. 
Client
   - Login Page
        - UI
        - sent -u -p to server
        - get success or fail, show related UI
- Server
  - Login
        - get -u -p from client side, compare return result